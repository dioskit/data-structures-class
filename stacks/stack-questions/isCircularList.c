/****
 * check if the linked list is circular
 * use two pointer:
 * fast pointer and slow pointer
 * while(fast pointer != NULL && slow pointer != NULL){
 *      move fast pointer by two step and slow with tow step
 *      if(fastpointer == pointer){
 *          print( list is circular )
 *          exit
 *      }
 * }
 *  
*/